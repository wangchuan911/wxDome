<!--index.wxml-->

  <i-row >
    <i-col span="4" offset="20" i-class="topHead">
      <!-- <image bindtap="bindViewTap"  open-type="getUserInfo" class="userinfo-avatar" src="{{userInfo.avatarUrl}}" mode="cover" bindtap="mine"></image-->
      <i-avatar src="{{userInfo.avatarUrl}}" size="large" shape="circle" open-type="getUserInfo" bindtap="mine"></i-avatar>
    </i-col>
  </i-row>
<view class="main" >
  <!-- <view class="userinfo">
    <button wx:if="{{!hasUserInfo && canIUse}}" open-type="getUserInfo" bindgetuserinfo="getUserInfo"> 获取头像昵称 </button>
    <block wx:else>
      <image bindtap="bindViewTap" class="userinfo-avatar" src="{{userInfo.avatarUrl}}" mode="cover"></image>
      <text class="userinfo-nickname">{{userInfo.nickName}}</text>
    </block>
  </view>
  <view class="usermotto">
    <text class="user-motto">{{motto}}</text>
  </view> -->

  <!--水波纹进度条 测试样例1-->
  <!-- <div class="wave-progress">
    <div class="inner">
      <div class="water" style="top:{{ top }}"></div>
      <div class="tips" style="position: inherit;width: inherit;height: inherit;">{{ tips }} </div>
    </div>
  </div> -->
 <canvas canvas-id='canvasArcCir' style="width: 200px; height: 200px;" bindtap="{{ isBook?'mileStoneBut':'bookBut'}}"></canvas>
 
</view>
